# DOSBox-X test configuration for integration tests
[sdl]
autolock=false

[dosbox]
quit warning=false
# Enable remote debugging
gdbserver=true
gdbserver port=2159
qmpserver=true
qmpserver port=4444

[autoexec]
# Mount the test assets directory
mount t: tests/integration/assets
